# ============================================================================
# Service: ZooKeeper Kustomization
# Description: Groups ZooKeeper manifests for base environment deployment.
# ============================================================================
resources:
  - configmap.yaml
  - service.yaml
  - statefulset.yaml
