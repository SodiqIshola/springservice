# ============================================================================
# Service: Kafka Kustomization
# Description: Groups Kafka broker manifests for base environment deployment.
# ============================================================================
resources:
  - configmap.yaml
  - service.yaml
  - statefulset.yaml
